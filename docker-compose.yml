services:
  backend:
    build: ./backend
    container_name: gerenciador-backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: gerenciador-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
